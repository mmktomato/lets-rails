<% breadcrumb :blogs %>

<%= render(
      'shared/list_header',
      headding: 'Blogs',
      new_item_url: new_blog_path,
      new_item_link: 'create new blog') %>

<%= render(
      'shared/list_items',
      items: @blogs,
      create_detail_url: -> (blog){ blog_path(blog.id) },
      create_show_url: -> (blog){ site_index_path(blog_screen_name: blog.screen_name) },
      create_delete_url: -> (blog){ blog_path(blog.id) })
%>
